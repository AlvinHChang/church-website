/*
 * SermonPage Messages
 *
 * This contains all the text for the SermonPage container.
 */
import { defineMessages } from 'react-intl';

export const scope = 'app.containers.SermonPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SermonPage container!',
  },
});
